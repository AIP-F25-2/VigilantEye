"""
Test Constants for VIGILANTEye Test Suite

NOTE: These credentials are for testing purposes only and are not used in production.
All values are test-only and do not represent real credentials.
"""

# Test password constants (for testing only, not production secrets)
TEST_PASSWORD = "test_password_123"  # Test-only password, not a real credential
TEST_WRONG_PASSWORD = "wrong_test_password"  # Test-only invalid password
TEST_SHORT_PASSWORD = "12345"  # Test-only invalid password for validation testing
TEST_STRONG_PASSWORD = "p@ssw0rd!123"  # Test-only strong password for validation testing

# Test user constants
TEST_USERNAME = "testuser"
TEST_EMAIL = "test@example.com"
TEST_INVALID_EMAIL = "invalid-email"

